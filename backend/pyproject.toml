[project]
name = "ahcof-backend"
version = "0.1.0"
description = "Backend API for the AHCoF member application prototype"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic-settings",
    "sqlalchemy",
    "psycopg[binary]",
    "alembic",
    "pydantic[email]",
    "pytest",
    "httpx",
]